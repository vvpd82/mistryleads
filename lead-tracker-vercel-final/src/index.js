
import React from "react";
import ReactDOM from "react-dom/client";
import LeadTracker from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <LeadTracker />
  </React.StrictMode>
);
